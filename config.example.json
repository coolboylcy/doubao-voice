{
  "_comment": "拷到 ~/.voice-doggo/config.json 并 chmod 600。默认 backend 是 funasr：本机离线推理，免费、不联网，模型随 App 携带，命令行使用前先跑 doggo fetch-model。只有想改走火山引擎豆包云端时才需要下面那组凭证——把 backend 改成 doubao，并按控制台签发的形态填 api_key，或 app_id + access_key。resource_id 与 endpoint 已指向 2.0 的可用组合，别改。",

  "backend": "funasr",

  "language": "zh-CN",
  "enable_itn": true,
  "enable_punc": true,

  "long_press_ms": 300,
  "max_recording_seconds": 120,
  "silence_cancel_ms": 3000,
  "voice_threshold": 500,

  "_doubao_only": "以下字段只在 backend 为 doubao 时用到",
  "api_key": "",
  "app_id": "",
  "access_key": "",
  "resource_id": "volc.seedasr.sauc.duration",
  "endpoint": "wss://openspeech.bytedance.com/api/v3/sauc/bigmodel_nostream"
}
